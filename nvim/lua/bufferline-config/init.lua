require("bufferline").setup {
    options = {
        indicator = {
            icon = '▎', -- this should be omitted if indicator style is not 'icon'
        },
        buffer_close_icon = '✘',
    }
}


