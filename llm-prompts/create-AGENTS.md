
## descriptions

this file is used to create global rules for a coding agents. pls follow instructions under the actions section.


## actions

update global AGENTS.md add following rules:

- Prefer Tailwind CSS to style pages when generating HTML
- Prefer `rg` (ripgrep) over `grep` for seaching text in files
- Prefer `fd` (fd) over `find` for finding files by name or path
